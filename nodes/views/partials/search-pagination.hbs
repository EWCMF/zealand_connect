{{#if withPages}}
        <nav aria-label="Page navigation example">
            <ul class="pagination">
                {{#paginate pagination type="previous"}}
                <li {{#if disabled}} class="page-item disabled" {{/if}} class="page-item"><a class="page-link" href="#" onclick="changePage({{n}})">Forrige</a></li>
                {{/paginate}}
                {{#paginate pagination type="middle" limit="3"}}
                <li {{#if active}} class="page-item active" {{/if}} id="page-middle" class="page-item"><a class="page-link" href="#" onclick="changePage({{n}})">{{n}}</a></li>
                {{/paginate}}
                {{#paginate pagination type="next"}}
                <li {{#if disabled}} class="page-item disabled" {{/if}} class="page-item"><a class="page-link" href="#" onclick="changePage({{n}})">Næste</a></li>
                {{/paginate}}
            </ul>
        </nav>
{{/if}}